<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zyg.dao.BookMapper">
    <insert id="saveBook">
        insert into book_inf values (null, #{title}, #{author}, #{price})
    </insert>
    <select id="findById" resultType="book">
        select book_id id, book_title title, book_author author, book_price price
        from book_inf where book_id = #{id}
    </select>
    <select id="findAll" resultType="book">
        select book_id id, book_title title, book_author author, book_price price
        from book_inf
    </select>
    <select id="findByName" resultType="book">
        select book_id id, book_title title, book_author author, book_price price
        from book_inf where book_title = #{name}
    </select>
    <update id="updateBook">
        update book_inf set book_title = #{title}, book_author = #{author}, book_price = #{price}
        where book_id = #{id}
    </update>
    <delete id="deleteBook">
        delete from book_inf
        where book_id = #{id}
    </delete>
</mapper>